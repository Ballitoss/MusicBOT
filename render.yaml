services:
  - type: worker
    name: music-bot
    env: python
    buildCommand: pip install -r requirements-light.txt
    startCommand: python3 continuous_monitor.py
    autoDeploy: true
    envVars: []
